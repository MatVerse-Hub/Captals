{
  "name": "@omega-capitals/contracts",
  "version": "1.0.0",
  "description": "Omega Capitals Smart Contracts - Solidity contracts for portfolio risk management",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run ../scripts/deploy-local.js --network hardhat",
    "deploy:testnet": "hardhat run ../scripts/deploy-testnet.js --network amoy",
    "deploy:mainnet": "hardhat run ../scripts/deploy-mainnet.js --network polygon",
    "verify": "hardhat verify --network amoy",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.1",
    "hardhat": "^2.19.5",
    "dotenv": "^16.4.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
